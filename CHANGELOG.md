# Changelog

## 0.1.3

- Added framework-aware CLI flows for `detect`, `build`, `verify`, `routes inspect`, and `deploy`.
- Added deployment history commands for listing, inspecting, promoting, and rolling back multi-route deployments.
- Kept the existing single-page upload workflow intact while expanding support for exported frontend app outputs.
